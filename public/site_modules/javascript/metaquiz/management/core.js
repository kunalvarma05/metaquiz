/**
 * Javascript for the Mangement Area
 * @return {[type]} [description]
 */
jQuery(document).ready(function() {
	jQuery('select[multiple]').multiselect({
		buttonWidth: "100%",
		maxHeight: 400
	});
});